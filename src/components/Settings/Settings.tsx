import React from "react";
import './index.css';

interface Props {

};

export const Settings:React.FC<Props> = ({}) => {
    return(
        <div className="Settings">
            
        </div>
    )
}